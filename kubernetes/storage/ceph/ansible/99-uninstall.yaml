---
- name: "Create LVM DaemonSet"
  hosts: "{{ k8s_host | default('localhost') }}"
  gather_facts: "{{ gathering_host_info | default('true') | bool == true }}"

  pre_tasks:

  tasks:
  - name: "Remove cephfs namespace"
    kubernetes.core.k8s:
      name: "{{ ceph_ns }}"
      api_version: v1
      kind: Namespace
      state: absent
...
