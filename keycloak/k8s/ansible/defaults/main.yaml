app_name: keycloak
app_namespace: keycloak
app_version: "26.1.3"

db_name: keycloak
db_user: keycloak
db_type: postgresql
# storage_class: local-storage

volumes:
  - name: postgresql
    capacity: 10Gi
    owner_user: 26
    owner_group: 26
    component: database

db_image_tag: 17
